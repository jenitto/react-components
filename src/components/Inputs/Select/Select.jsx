import React from 'react';
import PropTypes from 'prop-types';

const Select = ({ label, disabled, message }) => {

	return (
		<div>Select</div>
	);
}

Select.propTypes = {
};

export default Select;